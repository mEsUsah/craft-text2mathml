{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    first: true,
    instructions: 'Input - Enter a LaTeX formula (e.g. 1/R_p = 1/R_1 + 1/R_2). This can be copied from MS Word or generated using an online LaTeX editor.',
    name: field.handle,
    value: input,
}) }}

<div class="field">
    <div class="instructions">
        <p >Output  - Updates after save</p>
    </div>
    <div style="width: 100%; font-size: 1.5rem; border-radius: 3px; padding: 1.5rem; overflow: auto; background: #345e34; color: white;">
        {{output|raw}}
    </div>
</div>